# Copy to .env and set your real key
# DO NOT commit backend/.env to git.
OPENAI_API_KEY=sk-REPLACE_WITH_YOUR_KEY
# Optional: the model to use (gpt-4, gpt-4o, gpt-3.5-turbo, etc.)
OPENAI_MODEL=gpt-4
PORT=3000